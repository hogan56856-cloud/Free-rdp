name: RDP-Germany

on:
  workflow_dispatch:

jobs:
  secure-rdp:
    runs-on: ubuntu-latest # تحويل السيرفر إلى لينكس لضمان قيامه في مراكز بيانات أوروبا (المانيا/أيرلندا) وللحصول على سرعة تحميل تتخطى 2 جيجا!
    timeout-minutes: 360

    steps:
      - name: Update Package List & Install Desktop Environment
        run: |
          sudo apt-get update
          # تثبيت واجهة رسومية خفيفة جداً وسريعة (XFCE) مع خادم الـ RDP (XRDP)
          sudo apt-get install -y xfce4 xfce4-goodies xrdp
          
      - name: Configure XRDP Session
        run: |
          echo xfce4-session > ~/.xsession
          sudo systemctl enable xrdp
          sudo systemctl restart xrdp

      - name: Configure Core Firewall Settings
        run: |
          # فتح بورت الـ RDP في جدار الحماية للينكس
          sudo ufw allow 3389/tcp
          
      - name: Create RDP User with Secure Password
        id: create_user
        run: |
          # توليد كلمة مرور عشوائية قوية وآمنة
          PASSWORD=$(openssl rand -base64 12 | tr -d '/+=')
          # إنشاء مستخدم باسم RDP وإضافته لمجموعة الصلاحيات الكاملة (sudo)
          sudo useradd -m -s /bin/bash RDP
          echo "RDP:$PASSWORD" | sudo chpasswd
          sudo usermod -aG sudo RDP
          echo "RDP_CREDS=User: RDP | Password: $PASSWORD" >> $GITHUB_ENV
          echo "User: RDP | Password: $PASSWORD"

      - name: Install Tailscale
        run: |
          curl -fsSL https://tailscale.com/install.sh | sh

      - name: Establish Tailscale Connection
        run: |
          sudo tailscale up --authkey=${{ secrets.TAILSCALE_AUTH_KEY }} --hostname=gh-runner-$GITHUB_RUN_ID --accept-routes --accept-dns
          TS_IP=""
          retries=0
          while [ -z "$TS_IP" ] && [ $retries -lt 20 ]; do
              TS_IP=$(tailscale ip -4)
              sleep 5
              retries=$((retries+1))
          done
          if [ -z "$TS_IP" ]; then
              echo "Tailscale IP not assigned. Exiting."
              exit 1
          fi
          echo "TAILSCALE_IP=$TS_IP" >> $GITHUB_ENV

      - name: Verify Geographic Location is Germany/Europe
        run: |
          LOCATION_INFO=$(curl -s "https://ipinfo.io/json")
          IP=$(echo "$LOCATION_INFO" | grep -oP '"ip": "\K[^"]+')
          COUNTRY=$(echo "$LOCATION_INFO" | grep -oP '"country": "\K[^"]+')
          REGION=$(echo "$LOCATION_INFO" | grep -oP '"region": "\K[^"]+')
          CITY=$(echo "$LOCATION_INFO" | grep -oP '"city": "\K[^"]+')
          
          echo "Current Server IP: $IP"
          echo "Current Country: $COUNTRY"
          echo "Current Region/City: $REGION - $CITY"
          
          if [ "$COUNTRY" != "DE" ] && [ "$COUNTRY" != "NL" ] && [ "$COUNTRY" != "IE" ]; then
              echo "::warning:: The server is not in Germany/Europe! It is in $COUNTRY."
          else
              echo "Success: Server is successfully running from Europe! 🇪🇺 (Country: $COUNTRY)"
          fi

      - name: Verify RDP Accessibility
        run: |
          echo "Tailscale IP: $TAILSCALE_IP"
          # التأكد من أن بورت الـ RDP شغال ومفتوح للاتصال
          nc -z -v -w5 $TAILSCALE_IP 3389

      - name: Maintain Connection
        run: |
          echo -e "\n=== RDP ACCESS ==="
          echo "Address: $TAILSCALE_IP"
          echo "$RDP_CREDS"
          echo -e "==================\n"
          while true; do
              echo "[$(date)] RDP Active - Use Ctrl+C in workflow to terminate"
              sleep 300
          done
